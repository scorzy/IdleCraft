import { GameState } from '../../game/GameState'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function execCharNextAction(state: GameState, _charId: string): GameState {
    return state
}
